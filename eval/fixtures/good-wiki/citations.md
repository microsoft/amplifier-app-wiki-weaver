# Citations Page

Post-#25 rendering: `_render_provenance_def` renders the registry's captured
`author` + `url` straight into the footnote definition, so a reader actually
sees who wrote it and where to find it -- not a bare de-slugged filename.

Coding-assistant patterns draw on one source.[^1] Evaluation methodology
draws on another.[^2] Fine-tuning trade-offs are covered by a third.[^3]

## Sources

[^1]: Alice Chen — "a" — https://example.com/alice/a
[^2]: Bob Smith — "b" — https://example.com/bob/b
[^3]: Carol Wang — "c" — https://example.com/carol/c
